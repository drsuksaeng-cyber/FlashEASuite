# Master Instructions: FlashEASuite V2 Final Consolidation & Phase P9-5

## Task 1: Context & Requirement Hierarchy Analysis
1. Scan and read all text-based files in the repository to establish a Requirement Hierarchy.
2. **Rule of Recency:** Newer requirements and contexts override older ones. If a feature or logic in earlier documentation conflicts with later updates, the later version is the "Truth." Mark outdated features as "Expired."
3. Update the final project objectives and latest requirements based on this hierarchical scan.

## Task 2: Code-Context Alignment Audit
1. Audit every `.mq5` (MQL5) and `.py` (Python) file in the project.
2. Verify if the current implementation aligns with the requirements established in Task 1.
3. Identify and log any "Context Conflicts" where the code follows old logic that has been superseded by new requirements.

## Task 3: Comprehensive Documentation & Flow Mapping
1. Summarize the global context and system architecture.
2. Generate a detailed:
   - **Flowchart/Workflow:** Process logic from Feeder to Brain to Trader.
   - **Dataflow:** Movement of tick data, OHLC, ZMQ messages, and InfluxDB records.
   - **Technical Manual:** A highly detailed manual covering all system components.

## Task 4: Execute Phase P9-5 (Monitoring & Validation)
**Context:** Root directory is `FlashEASuite_V2/`.
**Reference Files:**
- `performance_tracker.py`, `execution_listener.py`, `config_builder.py`, `strategy_council.py`, `MQL5_LESSONS_LEARNED_P8_4.md`

### Sub-tasks:
1. **Develop `dashboard.py` (Terminal Dashboard):**
   - Use the `rich` library. 
   - Displays: Active strategies, Market regime, P&L, ZMQ status, Last config push, Trades today.
   - Refresh rate: 5 seconds.

2. **Fix & Finalize `tools/validate_live_readiness.py`:**
   - **Debugging:** Fix all existing errors when running the validation scripts.
   - **Tests:** Must cover ZMQ port connectivity, InfluxDB I/O, Dry-run CONFIG_PUSH, and StrategyManager reception.
   - Ensure the following commands work without errors:
     - `python tools\validate_live_readiness.py`
     - `python tools\validate_live_readiness.py --quick`
     - `python tools\validate_live_readiness.py --zmq`
     - `python tools\validate_live_readiness.py --influx`

3. **Generate `PRODUCTION_READY_CHECKLIST.md`:**
   - Create a checklist covering all system health points (Python Brain, FeederEA, ProgramC_Trader, Trade Reports, InfluxDB, Decision Logging, Health Monitor, S07 Baseline, Emergency Stop).
   - Include detailed instructions on how to interpret validation results.

## Final Goal: 
Resolve all bugs and ensure the system is 100% PASS for live deployment.

## Task 5: Project Cleanup & Documentation Management
1. **Identify Junk Files:** Scan all subdirectories for temporary files, old backups (e.g., .bak, .tmp), and redundant test scripts that are no longer referenced in the current Phase P9-5.
2. **Archive Execution:** - Create a directory named `_ARCHIVE_` at the project root.
   - Move all identified non-essential files to `_ARCHIVE_`, maintaining a flat structure if possible.
3. **Documentation Consolidation:**
   - Ensure all essential manuals and design docs are moved/kept in a dedicated `docs/` folder.
   - Delete any duplicate or outdated documentation files from other subdirectories to prevent "Context Pollution."